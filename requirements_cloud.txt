# ========== 激活验证服务器 - 云端部署依赖 ==========
# 轻量化配置，专注于激活码验证功能

# Web框架（必需）
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# HTTP客户端（必需）
requests==2.31.0

# 环境变量管理（推荐）
python-dotenv==1.0.0
